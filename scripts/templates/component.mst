import React, { Component } from 'react';

class {{name}} extends Component {
  render() {
    return (
      <div>{{name}}</div>
    )
  }
}

{{name}}.displayName = '{{name}}'

{{name}}.propTypes = {
  
}

{{name}}.defaultProps = {
  
}

export default {{name}}
